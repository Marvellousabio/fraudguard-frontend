# FraudGuard Frontend Environment Variables
# Copy this file to .env and fill in your values

# Backend API base URL (used in production builds)
# In dev, Vite proxy handles routing, so this can be empty
VITE_API_BASE_URL=backend url

# Mock mode toggle
VITE_MOCK_MODE=true
